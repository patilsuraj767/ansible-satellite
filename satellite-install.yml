---
- hosts: localhost
  gather_facts: no
  tasks:
    - name: Deploy VM
      tags: deploy-vm   
      include_role:
        name: rhevdeploy
      
- hosts: "{{ hostvars['localhost']['vmnicip'].matches[0].address }}"
  tasks:
    - name: configure VM for installing satellite 
      tags: configure-vm
      include_role:
        name: configurevm

    - name: Installing satellite
      tags: satellite-installer
      include_role:
        name: satellite-installer
      